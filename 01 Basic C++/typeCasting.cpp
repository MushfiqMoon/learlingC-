#include <iostream>
// #include<stdio.h>
using namespace std;
int main()
{
    char x;
    cin >> x;

    long long int ls = (long long int)(x);
    // OR
    cout << (long long int)(x) << endl;

    // cout << ls << endl;

    return 0;
}